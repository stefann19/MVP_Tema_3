<Window x:Class="MVP_Tema_3.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVP_Tema_3"
        xmlns:viewModels="clr-namespace:MVP_Tema_3.ViewModels"
        mc:Ignorable="d"
        Title="Login" SizeToContent="WidthAndHeight" MinWidth="300">
    <Window.DataContext>
        <viewModels:LoginViewModel></viewModels:LoginViewModel>
    </Window.DataContext>
    <StackPanel>
        <TextBlock Text="Username:"></TextBlock>
        <TextBox Text="{Binding Username,UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <TextBlock Text="Password:"></TextBlock>
        <TextBox Text="{Binding Password,UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <Button Command="{Binding LoginCommand}" Content="Login"></Button>
    </StackPanel>
</Window>
